apiVersion: v1
kind: Service
metadata:
  name: {{ include "mon-app.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mon-app.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}

